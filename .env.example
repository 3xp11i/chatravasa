# =====================================================
# Environment Variables for Chatravasa App
# =====================================================
# Copy this file to .env.local and fill in the values
# =====================================================

# Supabase Configuration
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_anon_key
NUXT_SUPABASE_SERVICE_KEY=your_supabase_service_role_key

# =====================================================
# Push Notifications (VAPID Keys)
# =====================================================
# Generate new keys by running: npx web-push generate-vapid-keys
# The public key is exposed to the client for subscription
# The private key is kept secret on the server for sending notifications

# Public key - exposed to client
NUXT_PUBLIC_VAPID_PUBLIC_KEY=your_vapid_public_key

# Private key - server only, never expose to client
NUXT_VAPID_PRIVATE_KEY=your_vapid_private_key

# Subject - typically a mailto: or https: URL identifying your app
NUXT_VAPID_SUBJECT=mailto:admin@chatravasa.com

# FILE: .env.example
# PURPOSE: Example environment variables for Manav Boys Hostel Meal Management app.
# INSTRUCTIONS: Copy this file to `.env` and fill in your actual values.
# SECURITY: Never commit .env to version control!

# ---------------------------------------------------------------------------
# Supabase Configuration
# Get these from your Supabase project settings → API
# ---------------------------------------------------------------------------
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key
SUPABASE_SERVICE_KEY=your-service-role-key-keep-secret

# ---------------------------------------------------------------------------
# Google OAuth (configured in Supabase Auth settings)
# ---------------------------------------------------------------------------
# Google OAuth is configured in Supabase dashboard, not here.
# But if using custom OAuth flow:
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# ---------------------------------------------------------------------------
# App Configuration
# ---------------------------------------------------------------------------
# Base URL for the app (used for OAuth callbacks)
NUXT_PUBLIC_SITE_URL=http://localhost:3000

# Meal cutoff times (24-hour format, optional override)
# MEAL_CUTOFF_BREAKFAST=07:00
# MEAL_CUTOFF_LUNCH=11:00
# MEAL_CUTOFF_DINNER=18:00
